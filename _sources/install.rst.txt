Installation
============

Dask-Optuna can be installed using ``pip``::

    pip install dask-optuna

The development version can be installed directly from GitHub::

    pip install git+https://github.com/jrbourbeau/dask-optuna