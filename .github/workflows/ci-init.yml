name: Initial workflow

on:
  push:
    branches: master
  pull_request:
    branches: master

jobs:
  checks:
    runs-on: ubuntu-latest
  
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Hello, world!
      run: echo "Hello, world!"